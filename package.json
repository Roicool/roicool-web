{
  "name": "roicool-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Roicool Webflow sitesinin davranış ve davranışsal stil katmanı",
  "repository": {
    "type": "git",
    "url": "https://github.com/Roicool/roicool-web.git"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "version": "npm run build && git add -A",
    "test": "node --test 'src/**/*.test.js'",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "prettier": "^3.4.2"
  }
}
